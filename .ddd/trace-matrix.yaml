# DDD Trace Matrix
# Generated view of the evidence graph: claims ↔ constructs ↔ sources.
# Updated by: ddd-ground skill (trace step), regenerated by ddd-book (validate).

schema_version: 0.1.0

# Each entry maps a code construct to the claims that support it.
# Every L2 construct MUST trace to at least one L1 claim or T0 exemption.
# Every L1 claim MUST trace to at least one L0 source OR explicit exception.
#
# Sample entry (commented out):
#
# - id: CL-001
#   schema_version: 0.1.0
#   symbol: src/domain/order/Order.ts#cancelOrder
#   symbol_kind: method       # method | class | module | function | component | boundary
#   claims: [C-055, C-017]
#   passport: order.aggregate
#   coverage_status: covered  # covered | t0-exempt | uncovered
#   coverage_reason: null     # required if t0-exempt or uncovered

traces: []
